# research 目录

用于承载研究、调参、验证、候选策略探索和非主线实验脚本。

当前状态：目录框架已建立，候选清单由 `quant/main/tools/repo_noise_report.py` 生成。

迁入规则：

- 必须由责任智能体认领。
- 必须经审计智能体确认不影响当前标准链路。
- 不得把 production 默认入口迁入本目录。
- 如确认无用，应进入项目回收站，而不是直接删除。
