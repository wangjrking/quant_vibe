# Quant MCP runtime requirements
#
# Canonical runtime:
#   D:\work\quant\quant_mcp\.venv\Scripts\python.exe
#   Python 3.9.23
#
# Notes:
# - Do not build this project with Python 3.13; alphalens 0.4.0 is not compatible.
# - pandas-market-calendars 5.x uses Python 3.10+ syntax, so keep it on 4.x for Python 3.9.
# - greenlet is pinned to a Windows binary wheel to avoid requiring local C++ Build Tools.

# Core data processing
pandas>=1.5.0,<3.0.0
numpy>=1.21.0,<2.1.0
pyarrow>=10.0.0,<22.0.0
fastparquet>=2023.0.0,<2025.0.0
openpyxl>=3.1.0

# Machine learning
xgboost>=1.6.0,<3.0.0
scikit-learn>=1.0.0,<1.7.0
shap>=0.40.0,<0.50.0

# Factor analysis and mining
alphalens==0.4.0
gplearn>=0.4.0,<0.5.0

# Data source
tushare>=1.2.0
pandas-market-calendars>=4.0.0,<5.0.0

# Technical indicators
ta>=0.10.0

# Database
sqlalchemy>=1.4.0,<3.0.0
greenlet==3.1.1

# Visualization and reports
tqdm>=4.62.0
matplotlib>=3.5.0,<4.0.0
Pillow>=9.0.0
python-docx>=1.1.0

# Optional deep-learning modules are installed by Docker build arg:
# docker build --build-arg INSTALL_TORCH=true -t quant-vibe .
