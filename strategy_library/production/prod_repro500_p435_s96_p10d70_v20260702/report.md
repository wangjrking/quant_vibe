# 复现500高收益策略生产归档说明

## 结论

策略 `prod_repro500_p435_s96_p10d70_v20260702` 已按用户授权发布为生产策略归档。掘金复现结果：年化 `5.004968`，Sharpe `3.905870`，最大回撤 `0.038695`。

## 规则摘要

- 基础候选：`w25_25_00_50_gapm8p3_top8`
- 买入 TopN：`3`
- 仓位：单票目标 `0.435`
- 过滤：`signal_pct_chg <= -1.75%`、`buy_open_gap_qfq <= 1.5%`、`pred_10d >= 0.70`、`pred_1d >= -0.002`
- 持仓：`1` 日，最大 `1` 日
- 卖出：`score_exit_entry_ratio=0.96`，`min_holding_days_before_score_exit=1`
- 禁止：北交所、ST/风险警示、退市、涨停不可买

## 当前信号状态

本归档最新历史信号为 `signal_date=20260528` / `buy_date=20260529`，状态为 `stale_no_current_signal`。该信号不得作为当前交易日 L7 交易输入；需要另行运行当前 L4 active formal 资产下的增量信号生成。

## 字段口径

模型、因子和技术派生字段可以使用显式 `_qfq` 字段。交易可执行过滤必须使用不复权真实价格，不得用前复权价格判断涨跌停或真实成交。
