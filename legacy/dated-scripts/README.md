# 带日期的一次性脚本归档

此目录保存曾经放在 `quant/main` 根目录、文件名带有具体日期的执行、构建、调参、验证与导出脚本。

这些文件是历史实验和阶段性排障的源代码留痕，不是当前 L1-L8 标准链路的默认入口。当前运行请以 `workflows/standard_incremental_trading_signal_l1_l8.json`、`docs/governance/` 和根目录中未归档的正式入口为准。

迁入条件：脚本带日期，且未被测试、配置、正式策略归档、复现清单或治理文档直接按路径引用。仍有显式引用的少数脚本保留原位置，避免破坏历史证据链。

归档脚本只能在明确的研究或复现场景中使用；运行前必须重新确认数据版本、依赖与资产写入边界。
